export enum FormFieldSize {
    ExtraSmall = 1,
    Small,
    Medium,
    Large,
}

export enum FormFieldType {
    Input = 1,
    Select,
    TextArea,
    Radio,
    Checkbox,
    Picture,
    File
}

export enum FormSelectFieldType {
    None = 1,
    Country,
    State,
    LGA
}