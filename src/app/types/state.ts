export interface StateListInterface {
    id: string;
    name: string;
    description: string;
    createdDate: string;
}

export interface StateFormInterface {
    id?: string;
    name: string;
    description: string;
}
