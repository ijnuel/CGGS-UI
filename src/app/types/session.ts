export interface SessionListInterface {
    id: string;
    name: string;
    description: string;
    createdDate: string;
}

export interface SessionFormInterface {
    id?: string;
    name: string;
    description: string;
}
