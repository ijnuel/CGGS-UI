export interface CountryListInterface {
    id: string;
    name: string;
    description: string;
    createdDate: string;
}

export interface CountryFormInterface {
    id?: string;
    name: string;
    description: string;
}
