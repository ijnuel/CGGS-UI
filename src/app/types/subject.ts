export interface SubjectListInterface {
    id: string;
    name: string;
    description: string;
    createdDate: string;
}

export interface SubjectFormInterface {
    id?: string;
    name: string;
    description: string;
}
